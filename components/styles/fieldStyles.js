import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    textInputField: {
        padding: 10,

        borderRadius: 4,
        borderWidth: 0.8,
        borderColor: '#000',
    }
})